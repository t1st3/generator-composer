language: node_js
node_js:
  - '0.8'
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-amd' ]; then cd .. && eval "mv $currentfolder generator-amd" && cd generator-amd; fi

